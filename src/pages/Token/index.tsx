import { Box } from '@mui/material';

const TokenPage = () => {
  return (
    <Box sx={{ display: 'flex', flexDirection: 'column', alignItems: 'center', marginTop: '88px' }}>
      Token Page
    </Box>
  );
};

export default TokenPage;
